# Core FastAPI Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.1.1
python-multipart==0.0.17

# Database
sqlalchemy==1.4.41
psycopg2-binary==2.9.9
asyncpg
# Allow resolver to pick a supabase version compatible with modern httpx
supabase>=2.4.0,<3.0.0

# Data Validation
pydantic==2.11.10
pydantic-settings==2.6.1
pydantic-core==2.33.2
email-validator==2.1.0

# Authentication & Security
python-jose[cryptography]==3.3.0
bcrypt==4.1.2

# Google Gemini AI (for native audio with Gemini 2.5 Flash)
google-genai==1.46.0
google-generativeai==0.8.5
google-ai-generativelanguage==0.6.15
google-api-core==2.25.2
google-auth==2.41.1

# LangChain & LangGraph
langchain==0.3.13
langchain-core==0.3.28
langchain-community==0.3.13
langchain-google-genai==2.0.8
langgraph==0.2.62

# HTTP Client (for CRM webhooks)
# ensure modern httpx for google-genai, chromadb, unstructured-client, etc.
httpx>=0.28.1,<1.0.0

# Underlying transport implementation (some libs require >=1.0.9)
httpcore>=1.0.9,<2.0.0

# Payment Processing
stripe==10.11.0

# PDF Processing & RAG
pymupdf==1.23.26
Pillow==10.2.0

# OpenAI (for OCR on scanned PDFs and text chat fallback)
openai==1.12.0

# Anthropic (for text chat - primary)
anthropic==0.74.0

# Vector Store & Embeddings
sentence-transformers==2.7.0
huggingface-hub==0.23.0
numpy==1.24.3

# Audio Processing (high-quality resampling for phone calls)
scipy==1.11.4
soxr==0.3.7  # High-quality audio resampler (better than scipy/audioop)

# Text Processing (optional)

# Web Scraping
beautifulsoup4==4.12.3
requests==2.31.0
html2text==2024.2.26

# Required dependencies for above packages
protobuf==5.29.5
cryptography==42.0.0

# Integration Dependencies
# Google APIs (for Calendar, Gmail)
google-api-python-client==2.150.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# Microsoft Graph API (for Outlook Calendar/Email)
msal==1.28.0

# Database connectors (already have psycopg2 for PostgreSQL)
pymongo==4.10.1  # MongoDB
redis==5.2.0  # Redis (if needed for integration)

# Email (SMTP is built-in, but adding for completeness)
aiosmtplib==3.0.2  # Async SMTP

# Additional OAuth/OAuth2 support
authlib==1.3.1

# Additional utilities
python-dateutil==2.9.0
# WebSockets used by google-genai
websockets>=13.0.0,<15.1.0
